export default function HomePage() {
  return (
    <main style={{ padding: '80px 24px', maxWidth: 720, margin: '0 auto', textAlign: 'center' }}>
      <h1 style={{ fontFamily: 'var(--f-display)', fontSize: 'var(--t-display-l)' }}>
        Upstate Home Sales
      </h1>
      <p style={{ marginTop: 16, color: 'var(--c-ink-soft)' }}>
        Public site is wired up but content lands in Week 3. See <code>/site/</code> in the
        prototype for the visual spec.
      </p>
    </main>
  );
}
